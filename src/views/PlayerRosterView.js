import PlayerRoster from "../components/PlayerRoster";

const PlayerRosterView = (props) => (
  <div className="roster-view">
    <PlayerRoster {...props} />
  </div>
);

export default PlayerRosterView;
